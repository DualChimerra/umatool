{
  "name": "paddock",
  "version": "1.0.0",
  "private": true,
  "description": "Champions Meeting preparation tool for Uma Musume Pretty Derby (Global)",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "build:data:offline": "node scripts/build-data.mjs --no-gametora",
    "build:icons": "python3 scripts/build_icons.py",
    "verify:model": "node scripts/verify-model.mjs",
    "serve": "python3 -m http.server 8080 --directory docs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
